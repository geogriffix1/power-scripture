{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Angular (auto serve)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "ng serve",
      "timeout": 15000,
    }
  ]
}
