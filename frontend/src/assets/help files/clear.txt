Import Console Clear Command

Syntax: clear

The clear command clears all output lines from the console. It removes
the clutter of previous operations, but it does not affect any operations.
Once cleared, the output is no longer available.

In a power scripture import file, clear has no affect.